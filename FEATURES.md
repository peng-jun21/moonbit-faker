# 功能与兼容性边界

0.4 在原 25 类标量 provider 上新增六地区词库、217 个公开地区/方法组合、嵌套/数组/引用/外表/拼接、持续唯一性、完整会话检查点、流式 Node/CLI 和可取消网页。旧 profile、标量配置和示例种子继续通过测试。

| 已支持且已有证据 | 当前边界 |
|---|---|
| 固定 Faker 40.39.0 数据、权重、选定格式规则 | 只有六地区的选定 provider；不是完整 locale 或任意方法/参数兼容 |
| 651 组官方 provider 输出对照、5208 个值一致 | 参考使用明确替换的 xorshift32 Random；默认 MT19937 不兼容 |
| object/array/ref/foreign/nullable/unique/join | 无用户回调、表达式语言、循环依赖或任意 JSON 对象唯一性 |
| 单表暂停恢复、跨表逐表生成 | 多表暂不支持恢复；被引用表需要有限缓存 |
| 背压、取消、超时、行失败回滚、文件防覆盖 | 数据与检查点并非两文件事务；stdout 失败可已有部分结果 |
| 十万行流式与固定种子分布检查 | 非密码随机源，非统计质量认证，无跨进程唯一性保证 |
| 四个同机官方任务计时与精确输出 hash | 子集、不同语言运行时及适配 RNG，不是通用性能追平 |
| 实际 IAB 页面、移动布局和下载解析 | 仅此 Windows/Node/Python/MoonBit 版本；远程 CI 未运行 |

本地化词库保留 Faker MIT 许可；条目数量包含共享回退和不同性别的重复项。原原创小词表 provider 继续存在，其地址/公司标有示例；localized 采用官方真实地名/姓名词汇，可能与现实重合。UUID 是 v4 格式，EAN/ISBN 是校验位样本，不验证实际注册分配。

完整上游 API/provider/地区、MT19937、扩展机制、完整官方测试、跨平台长期与峰值内存验证仍缺，**不能判定完整追平**。独立对照证据和限制一起保存在 TESTING.md。全部只在本地，旧压缩包未更新。
