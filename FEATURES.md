# 功能与兼容性边界

0.3 增加 25 类 provider 配置、JSON 标量数据表、JSON/JSONL/CSV、按列唯一性与可空值、加权/不放回采样、PRNG 状态恢复、区间日期和精确缩放小数。旧 profile 种子样例保持不变。

仍远未覆盖 Python Faker 的 provider/locale/词库规模；不含嵌套/关系数据、完整本地化、密码随机源、跨进程唯一性或统计分布保证。唯一性有重试上限，状态在每次 records 调用重新建立。数据表和输出有资源预算，暂不流式生成。

UUID 只符合 v4 位布局与格式；EAN/ISBN 只检查长度、978 前缀（ISBN）与校验位，不验证实际分配。没有真实身份号码生成能力。姓名可能与真人相同，地址/公司使用测试标记，邮箱/IP 为示例用途。

独立 Python 检查覆盖标准格式和序列化，未运行 Python Faker、未主张种子序列或大词库兼容。当前有功能进展，不能判定已追平。全部仅本地，旧压缩包未更新。
