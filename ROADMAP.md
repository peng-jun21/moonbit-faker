# 后续计划

0.4 已补六地区词库、结构和关系数据、持续流式与单表检查点、官方 provider 对照及有限统计/性能测量。

1. 扩展实际有需求的官方 provider、参数与更多地区；每个扩展都添加未修改官方 provider 的独立向量，明确回退规则。
2. 实现或明确分离 MT19937 种子兼容、上游 unique、多地区权重和扩展注册机制；保留现有种子接口。
3. 增加多数据集可恢复执行、外表索引/外部存储、更灵活的派生字段，同时定义事务与错误恢复边界。
4. 多种子、大语料、完整官方测试适用子集、长时间生成及真实峰值内存和跨平台检查；重复比较主流负载而非仅四个任务。
5. 最终验收后更新 ZIP/bundle 与统一归档，仍需用户授权才上传或发布。

尚未实现以上范围，当前仍不是完整 Python Faker 替代实现。
