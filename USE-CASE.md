# 关联客户和订单数据集

为客户—订单等关联表生成可重复的测试数据；批次或进程中断后继续生成时保持序列、外键关系和唯一性状态。

## 输入、操作、输出

原创合成 schema，词库来源仍为 Faker；不称真实数据脱敏。

最简运行：先按 README 构建，然后 `node examples/run-use-case.mjs`。它自动创建输出目录并执行下面命令。下列 `{out}` 是运行器替换的实际目录，不是直接输入 shell 的变量；stdin 文件由运行器传递，以避免 Windows 与 POSIX 重定向差异。

```text
node tools/stream.mjs --job examples/related-shop.json --out {out}/shop.jsonl
```

观察：新文件包含 100 个 customers 与 1000 个 orders；客户引用来自先生成的数据集。

每一步输出见实际目录下 `step-N.stdout.txt` / `step-N.stderr.txt`；本轮已保存回执见 `evidence/value-rework-20260922/use-case.json`。

## 为什么保留这个实现

需要同版本可重放、关联字段与可保存生成会话时选择；词库数量本身不是独立贡献。

本轮未找到同范围 MoonBit 数据生成库。价值在关联 schema、可保存状态和可复現数据工作流，不是发明随机数或姓名数据；词库及 Faker 来源版本必须披露。

## 不能由样例推出的结论

合成数据不是脱敏后的真实数据，不具备密码学随机性或跨进程全局唯一性保证；唯一值重试有上限。

该样例是可修改的使用入口，不能证明存在真实用户、全部兼容或性能领先。继续投入的依据应是明确的输入或接入需求；若对接任务用既有成熟库即可完成，应优先复用而不是为保留参赛数量扩张本项目。
